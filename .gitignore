# ── 运行产物与真实配置（绝不提交）────────────────
# NapCat 运行时（腾讯 QQ 二进制 + 原生库，319MB，版权原因不入仓库，完整包走 Release 附件）
app/napcat/
# 部署者运行后生成的真实配置（只提交 example 模板）
config/bridge.config.json
# DeepSeek Harness 的包内数据目录（部署后含 API Key/会话）
app/dsh/home/
# 运行日志
logs/
*.log
# 备份文件
*.bak
*.bak-*

# ── 依赖与系统杂项 ────────────────────────────────
node_modules/
Thumbs.db
Desktop.ini
.DS_Store
